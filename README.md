```
> what is it?
	A open Source CLIchat

> why is created?
	idk

> how to install?
	deps: [
		npm 
		bun
	];
	curl -fsSL https://bun.sh/install | bash
	npm install
	bun build ./client.js --compile --outfile clichat

> what are avilable commands?
	you can exit with '/exit' or '/quit'
```
